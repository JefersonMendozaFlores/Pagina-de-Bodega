@model IEnumerable<PrjProyecto_Tienda_Bodega.Models.Clientes>

@{
    ViewBag.Title = "ListadoClientes";
}

<h2>Listado de los Clientes</h2>

<p>
    @Html.ActionLink("Registrar nuevo cliente", "ClienteInsertar")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.idCliente)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.nombres)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.apellidos)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.fechaNacimiento)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.celular)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.idCliente)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.nombres)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.apellidos)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.fechaNacimiento)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.celular)
        </td>
        <td>
            @Html.ActionLink("Editar", "ActualizarCliente", new { id = item.idCliente }) 
        </td>
    </tr>
}

</table>

<br/>
<a href="http://localhost:61127/Menu.aspx">Regresar Al Menu</a>
